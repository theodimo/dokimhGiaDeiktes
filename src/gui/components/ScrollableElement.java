package gui.components;

public interface ScrollableElement {
    public int computeHeight(); //computes the height of that instance of the class that implements this interface. Used at resizing when we dynamically add components
}
